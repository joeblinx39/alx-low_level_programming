#include "main.h"

/**
 * print_numbers - prints integers from 0 - 9, then a new line
 */
void print_most_numbers(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		if (c != '2' && c != '4')
		{
			_putchar(i + '0');
		}
	}
	_putchar('\n');
}
