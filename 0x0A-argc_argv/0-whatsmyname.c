#include <stdio>

/**
 * main - Prints the name of a program
 *
 * @argc: number of command-line argument
 *
 * @argv: string of argument passed on main func
 *
 * Return: Always (0) (Success)
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
